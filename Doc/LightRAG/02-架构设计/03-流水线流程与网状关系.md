# LightRAG 流水线流程与网状关系

**项目**：LightRAG · **版本**：1.5.5 · **日期**：2026-07-10 · **作者**：15531

> 本文档用**网状关系图**梳理 LightRAG 流水线：组件之间如何互相依赖、数据如何在它们之间流动、谁调用谁、谁持有谁。全部基于源码核实。

---

## 一、全景网状关系图

一张图看清 LightRAG 的全部核心组件与依赖关系：

```mermaid
graph TB
    subgraph CORE["🎯 LightRAG 核心对象（lightrag.py）"]
        RAG["LightRAG 实例<br/>━━━━━━━━━━━━<br/>orchestrator + 8 存储持有者"]
    end

    subgraph MIXINS["Mixins 组成"]
        ROLE["_RoleLLMMixin<br/>━━━━━<br/>4 角色 LLM"]
        MIG["_StorageMigrationMixin<br/>━━━━━<br/>数据迁移"]
        PIPE["_PipelineMixin<br/>━━━━━<br/>文档处理管线"]
    end

    subgraph ROLES["🤖 角色化 LLM（llm_roles.py）"]
        EXTRACT["EXTRACT<br/>实体/关系抽取"]
        QUERY["QUERY<br/>问答生成"]
        KEYWORDS["KEYWORDS<br/>关键词抽取"]
        VLM["VLM<br/>多模态图像"]
    end

    subgraph STORAGES["💾 8 个存储实例（initialize_storages）"]
        direction LR
        FULL_DOCS["full_docs<br/>原文/解析产物"]
        DOC_STATUS["doc_status<br/>处理状态"]
        TEXT_CHUNKS["text_chunks<br/>分块文本"]
        FULL_ENT["full_entities<br/>实体详情"]
        FULL_REL["full_relations<br/>关系详情"]
        ENT_VDB["entities_vdb<br/>实体向量"]
        REL_VDB["relationships_vdb<br/>关系向量"]
        CHUNK_VDB["chunks_vdb<br/>块向量"]
        GRAPH["chunk_entity_relation_graph<br/>知识图谱"]
        CACHE["llm_response_cache<br/>LLM缓存"]
    end

    subgraph ENGINES["🔧 解析引擎（parser/registry）"]
        direction LR
        NATIVE["native"]
        LEGACY["legacy"]
        MINERU["mineru"]
        DOCLING["docling"]
    end

    subgraph CHUNKERS["✂️ 分块器（chunker/）"]
        direction LR
        F["F token_size"]
        R["R recursive"]
        V["V semantic_vector"]
        P["P paragraph_semantic"]
    end

    subgraph OPS["⚙️ 核心操作（operate.py）"]
        EXTRACT_FN["extract_entities"]
        MERGE_FN["merge_nodes_and_edges"]
        KG_QUERY["kg_query"]
        NAIVE_Q["naive_query"]
        KW["extract_keywords"]
    end

    RAG --> ROLE
    RAG --> MIG
    RAG --> PIPE
    ROLE --> EXTRACT & QUERY & KEYWORDS & VLM

    RAG ===|"持有"| STORAGES
    RAG ===|"路由"| ENGINES
    PIPE ===|"调用"| ENGINES
    PIPE ===|"调用"| CHUNKERS
    PIPE ===|"调用"| OPS

    EXTRACT --> EXTRACT_FN
    KEYWORDS --> KW
    QUERY --> KG_QUERY & NAIVE_Q

    EXTRACT_FN -->|"写"| ENT_VDB & REL_VDB & TEXT_CHUNKS & CHUNK_VDB
    MERGE_FN -->|"读写"| GRAPH & ENT_VDB & REL_VDB & FULL_ENT & FULL_REL
    KG_QUERY -->|"查"| ENT_VDB & REL_VDB & GRAPH & CHUNK_VDB
    NAIVE_Q -->|"查"| CHUNK_VDB

    NATIVE -->|"写"| FULL_DOCS
    LEGACY -->|"写"| FULL_DOCS

    style RAG fill:#fffde7,stroke:#f57f17
    style STORAGES fill:#e3f2fd,stroke:#1565c0
    style ROLES fill:#f3e5f5,stroke:#6a1b9a
    style ENGINES fill:#e8f5e9,stroke:#2e7d32
    style OPS fill:#fff3e0,stroke:#e65100
```

---

## 二、LightRAG 持有的 10 个存储实例

LightRAG 核心对象在 `initialize_storages()` 时实例化以下存储（`lightrag.py:1095-1145`），它们是整个系统的数据底座：

```mermaid
graph LR
    subgraph KV类["KV 存储（BaseKVStorage）"]
        FD["full_docs<br/>━━━━━━━<br/>原文 + 解析产物<br/>输入侧"]
        TC["text_chunks<br/>━━━━━━━<br/>分块后的文本<br/>含 tokens/content"]
        FE["full_entities<br/>━━━━━━━<br/>实体详情<br/>name/desc/source_id"]
        FR["full_relations<br/>━━━━━━━<br/>关系详情<br/>src/tgt/desc/weight"]
        LRC["llm_response_cache<br/>━━━━━━━<br/>LLM 调用缓存<br/>省成本"]
        DS["doc_status<br/>━━━━━━━<br/>文档状态机<br/>PENDING→PROCESSED"]
    end

    subgraph 向量类["向量存储（BaseVectorStorage）"]
        EV["entities_vdb<br/>━━━━━━━<br/>实体嵌入向量"]
        RV["relationships_vdb<br/>━━━━━━━<br/>关系嵌入向量"]
        CV["chunks_vdb<br/>━━━━━━━<br/>文本块嵌入向量"]
    end

    subgraph 图类["图存储（BaseGraphStorage）"]
        GR["chunk_entity_relation_graph<br/>━━━━━━━<br/>实体节点 + 关系边"]
    end

    style KV类 fill:#e3f2fd,stroke:#1565c0
    style 向量类 fill:#fff3e0,stroke:#e65100
    style 图类 fill:#e8f5e9,stroke:#2e7d32
```

### 各存储的读写角色

| 存储 | 谁写入 | 谁读取 |
|---|---|---|
| `full_docs` | parse_worker（解析后） | analyze/process worker、去重检查 |
| `doc_status` | enqueue、各 worker（状态转换） | 处理循环、scan、去重 |
| `text_chunks` | 分块阶段 | 抽取阶段、查询（chunk 内容） |
| `full_entities` | merge_nodes_and_edges | 图谱重建、查询 |
| `full_relations` | merge_nodes_and_edges | 图谱重建、查询 |
| `llm_response_cache` | extract/keywords/query（命中则跳 LLM） | 所有 LLM 调用前查 |
| `entities_vdb` | extract + merge（embed 入库） | kg_query（向量召回实体） |
| `relationships_vdb` | extract + merge（embed 入库） | kg_query（向量召回关系） |
| `chunks_vdb` | 分块阶段（embed 入库） | kg_query + naive_query（召回 chunk） |
| `chunk_entity_relation_graph` | merge_nodes_and_edges（upsert node/edge） | kg_query（图遍历取子图） |

---

## 三、入库流水线（写入侧）网状流程

文档从进入到知识图谱的完整流程，每一步标注**数据流向哪个存储**：

```mermaid
graph TD
    INPUT["📥 文档输入<br/>文件/文本/目录扫描"]

    INPUT --> ENQUEUE["apipeline_enqueue_documents<br/>━━━━━━━━━━━━━<br/>写 full_docs → 写 doc_status(PENDING)<br/>触发处理循环"]

    ENQUEUE --> LOOP["apipeline_process_enqueue_documents<br/>━━━━━━━━━━━━━<br/>扫描 PENDING → 调度 _run_pipeline_batch<br/>三级级联队列"]

    LOOP --> L1["Layer 1: _parse_worker"]

    subgraph L1_DETAIL["解析层"]
        L1 --> L1_ROUTING{"引擎路由<br/>process_options"}
        L1_ROUTING -->|"native/docx"| NATIVE_E["NativeDocxParser<br/>Markdown + blocks"]
        L1_ROUTING -->|"legacy"| LEGACY_E["LegacyParser<br/>纯文本"]
        L1_ROUTING -->|"mineru"| MINERU_E["MinerUParser<br/>Markdown + 全模态"]
        L1_ROUTING -->|"docling"| DOCLING_E["DoclingParser<br/>Markdown + 图片"]

        NATIVE_E --> L1_OUT["ParseResult<br/>content + blocks_path"]
        LEGACY_E --> L1_OUT
        MINERU_E --> L1_OUT
        DOCLING_E --> L1_OUT

        L1_OUT -->|"持久化"| FULL_DOCS_W[("full_docs")]
        L1 -->|"状态"| STATUS_P[("doc_status<br/>PARSING")]
    end

    L1_DETAIL --> L2["Layer 2: _analyze_worker"]

    subgraph L2_DETAIL["多模态分析层"]
        L2 --> L2_CHECK{"有图片 +<br/>i 选项 +<br/>VLM 配置?"}
        L2_CHECK -->|"否"| L2_SKIP["跳过<br/>analyzing_stage_skipped"]
        L2_CHECK -->|"是"| L2_VLM["_analyze_drawing<br/>VLM 生成图片描述<br/>注入 content"]
        L2 -->|"状态"| STATUS_A[("doc_status<br/>ANALYZING")]
    end

    L2_DETAIL --> L3["Layer 3: process_single_document"]

    subgraph L3_DETAIL["抽取+入库层"]
        L3 --> CHUNK["① 分块<br/>━━━━━━━━━<br/>F/R/V/P 策略选择"]
        CHUNK --> CHUNK_WRITE[("text_chunks<br/>chunks_vdb")]

        CHUNK --> EXTRACT["② 实体抽取<br/>━━━━━━━━━━━<br/>extract_entities<br/>EXTRACT 角色 LLM<br/>+ gleaning 多轮补抽<br/>+ cache 查询"]
        EXTRACT --> EXTRACT_CACHE[("llm_response_cache<br/>读/写")]
        EXTRACT --> EXTRACT_OUT["chunk_results<br/>实体+关系列表"]

        EXTRACT_OUT --> MERGE["③ 合并入库<br/>━━━━━━━━━━━<br/>merge_nodes_and_edges<br/>keyed lock 防竞争<br/>LLM 重摘要"]
        MERGE --> MERGE_ENT[("entities_vdb<br/>full_entities")]
        MERGE --> MERGE_REL[("relationships_vdb<br/>full_relations")]
        MERGE --> MERGE_GRAPH[("知识图谱<br/>node + edge")]

        MERGE --> DONE["_insert_done<br/>状态 → PROCESSED"]
        DONE --> STATUS_D[("doc_status<br/>PROCESSED")]
    end

    style INPUT fill:#fffde7,stroke:#f57f17
    style L1 fill:#e8f5e9,stroke:#2e7d32
    style L2 fill:#f3e5f5,stroke:#6a1b9a
    style L3 fill:#fff3e0,stroke:#e65100
```

---

## 四、查询流水线（读取侧）网状流程

查询时数据从各存储召回，最终组装成答案：

```mermaid
graph TD
    Q["🔍 用户查询<br/>aquery(query, mode)"]

    Q --> KW_STEP["关键词抽取<br/>━━━━━━━━━━━━<br/>extract_keywords<br/>KEYWORDS 角色 LLM"]
    KW_STEP --> KW_OUT["low_level_keywords<br/>high_level keywords"]
    KW_STEP --> KW_CACHE[("llm_response_cache<br/>读/写")]

    KW_OUT --> MODE{"mode 分支"}

    MODE -->|"local"| KG_L["kg_query<br/>(low_level 关键词)"]
    MODE -->|"global"| KG_G["kg_query<br/>(high_level 关键词)"]
    MODE -->|"hybrid"| KG_H["kg_query<br/>(low + high)"]
    MODE -->|"mix"| KG_M["kg_query<br/>+ naive_query 叠加"]
    MODE -->|"naive"| NAIVE["naive_query<br/>纯向量检索"]
    MODE -->|"bypass"| BYPASS["跳过检索<br/>直接问 LLM"]

    subgraph KG_INTERNAL["kg_query 内部（_perform_kg_search）"]
        direction TB
        KG_1["① 向量召回实体<br/>━━━━━━━━━━━━━<br/>entities_vdb.query(keywords)"]
        KG_2["② 向量召回关系<br/>━━━━━━━━━━━━━<br/>relationships_vdb.query(keywords)"]
        KG_3["③ 图遍历取子图<br/>━━━━━━━━━━━━━<br/>graph.get_node_edges<br/>get_knowledge_graph"]
        KG_4["④ 找最相关边/实体<br/>━━━━━━━━━━━━━<br/>_find_most_related_*"]
        KG_5["⑤ 召回相关 chunk<br/>━━━━━━━━━━━━━<br/>chunks_vdb.query<br/>_find_related_text_unit"]
        KG_6["⑥ 可选 rerank<br/>━━━━━━━━━━━━━<br/>重排候选"]

        KG_1 --> KG_3 --> KG_4
        KG_2 --> KG_4
        KG_4 --> KG_5 --> KG_6
    end

    KG_L & KG_G & KG_H & KG_M --> KG_INTERNAL

    subgraph NAIVE_INTERNAL["naive_query 内部"]
        NAIVE_1["chunks_vdb.query<br/>直接向量召回 chunk"]
        NAIVE_1 --> NAIVE_2["可选 rerank"]
    end
    NAIVE --> NAIVE_INTERNAL

    KG_INTERNAL --> CTX["_build_context_str<br/>━━━━━━━━━━━━━<br/>合并实体/关系/chunk<br/>按 token 预算截断"]
    NAIVE_INTERNAL --> CTX

    CTX --> GEN["QUERY 角色 LLM<br/>━━━━━━━━━━━━━<br/>context + query → 答案<br/>可 SSE 流式"]
    GEN --> GEN_CACHE[("llm_response_cache<br/>读/写")]
    GEN --> ANSWER["📝 答案（含引用）"]

    style Q fill:#fffde7,stroke:#f57f17
    style KG_INTERNAL fill:#e3f2fd,stroke:#1565c0
    style NAIVE_INTERNAL fill:#e8f5e9,stroke:#2e7d32
    style GEN fill:#fff3e0,stroke:#e65100
    style ANSWER fill:#c8e6c9,stroke:#2e7d32
```

---

## 五、模块依赖网

代码层面的 `import` 依赖关系（谁依赖谁）：

```mermaid
graph BT
    BASE["base.py<br/>━━━━━<br/>抽象基类<br/>存储契约 / QueryParam"]
    UTILS["utils.py<br/>━━━━━<br/>通用工具"]
    CONSTANTS["constants.py<br/>━━━━━<br/>常量/默认值"]
    PROMPT["prompt.py<br/>━━━━━<br/>PROMPTS 模板"]
    EXCEPTIONS["exceptions.py<br/>━━━━━<br/>异常定义"]

    KG["kg/<br/>━━━━━<br/>存储后端实现<br/>+ shared_storage<br/>+ factory"]
    PARSER["parser/<br/>━━━━━<br/>解析引擎<br/>+ registry + routing"]
    CHUNKER["chunker/<br/>━━━━━<br/>4 种分块策略"]
    SIDECAR["sidecar/<br/>━━━━━<br/>IR + write_sidecar"]
    LLM["llm/<br/>━━━━━<br/>LLM/Embedding 适配"]
    RERANK["rerank<br/>━━━━━<br/>重排序"]

    OPERATE["operate.py<br/>━━━━━<br/>抽取/合并/检索<br/>核心算法"]
    PIPELINE["pipeline.py<br/>━━━━━<br/>处理管线<br/>_PipelineMixin"]
    ROLES["llm_roles.py<br/>━━━━━<br/>4 角色 LLM 路由"]
    MIGRATION["storage_migrations.py<br/>━━━━━<br/>数据迁移"]
    ADDON["addon_params.py<br/>━━━━━<br/>参数管理"]

    LIGHTRAG["lightrag.py<br/>━━━━━<br/>LightRAG 门面<br/>+ dataclass"]

    API["api/<br/>━━━━━<br/>FastAPI 服务"]

    BASE --> KG & PARSER & CHUNKER & LLM & OPERATE
    UTILS --> OPERATE & PIPELINE & LIGHTRAG
    CONSTANTS --> PARSER & PIPELINE
    PROMPT --> OPERATE & PIPELINE
    EXCEPTIONS --> OPERATE & PIPELINE

    KG --> OPERATE
    PARSER --> PIPELINE
    SIDECAR --> PARSER
    CHUNKER --> PIPELINE & OPERATE
    LLM --> OPERATE & ROLES
    RERANK --> OPERATE

    OPERATE --> PIPELINE & LIGHTRAG
    ROLES --> LIGHTRAG
    MIGRATION --> LIGHTRAG
    ADDON --> LIGHTRAG

    PIPELINE --> LIGHTRAG
    LIGHTRAG --> API

    style BASE fill:#ffcdd2,stroke:#c62828
    style OPERATE fill:#fff3e0,stroke:#e65100
    style LIGHTRAG fill:#fffde7,stroke:#f57f17
    style API fill:#e8f5e9,stroke:#2e7d32
```

### 分层职责

```mermaid
graph TB
    subgraph L5["第 5 层：服务"]
        API["api/<br/>FastAPI + Ollama 兼容"]
    end

    subgraph L4["第 4 层：编排门面"]
        LIGHTRAG["lightrag.py<br/>LightRAG 门面<br/>用户入口"]
    end

    subgraph L3["第 3 层：业务逻辑"]
        PIPELINE["pipeline.py<br/>文档处理管线"]
        OPERATE["operate.py<br/>抽取/合并/检索"]
        ROLES["llm_roles.py<br/>4 角色路由"]
    end

    subgraph L2["第 2 层：领域组件"]
        PARSER["parser/<br/>解析引擎"]
        CHUNKER["chunker/<br/>分块策略"]
        SIDECAR["sidecar/<br/>IR 渲染"]
        LLM["llm/<br/>模型适配"]
        RERANK["rerank<br/>重排序"]
    end

    subgraph L1["第 1 层：基础设施"]
        KG["kg/<br/>存储后端"]
        BASE["base.py<br/>抽象契约"]
        UTILS["utils.py<br/>工具"]
        CONSTANTS["constants.py"]
        PROMPT["prompt.py"]
    end

    L5 --> L4 --> L3 --> L2 --> L1

    style L5 fill:#e8f5e9
    style L4 fill:#fffde7
    style L3 fill:#fff3e0
    style L2 fill:#f3e5f5
    style L1 fill:#e3f2fd
```

---

## 六、并发与锁的关系网

LightRAG 用多级锁协调并发，这是它处理大规模语料不锁死的关键：

```mermaid
graph TD
    subgraph LOCK1["全局锁（workspace 级）"]
        PL["pipeline_status lock<br/>━━━━━━━━━━━━━<br/>保护 busy/destructive_busy<br/>scanning/pending_enqueues<br/>等并发状态字段"]
        EQ["enqueue_serialize lock<br/>━━━━━━━━━━━━━<br/>序列化去重+upsert<br/>防止并发写入竞争"]
    end

    subgraph LOCK2["队列级并发"]
        PARSE_Q["parse_queues<br/>━━━━━━━━━━━━━<br/>每引擎组一个队列<br/>native/mineru/docling<br/>各独立并发上限"]
        SEM["max_parallel_insert<br/>━━━━━━━━━━━━━<br/>处理层信号量<br/>控制 LLM 抽取并发"]
    end

    subgraph LOCK3["实体级锁（keyed lock）"]
        ENT_LOCK["storage keyed lock<br/>━━━━━━━━━━━━━<br/>每个实体名/关系键一把锁<br/>同名串行 / 异名并行<br/>merge_nodes_and_edges 用"]
    end

    subgraph LOCK4["存储后端锁"]
        VDB_LOCK["向量库写锁<br/>━━━━━━━━━━━━━<br/>防并发 upsert 冲突"]
        GRAPH_LOCK["图存储锁<br/>━━━━━━━━━━━━━<br/>防并发 upsert_node/edge"]
    end

    PL -->|"协调|流水线状态"| EQ
    EQ -->|"保护|入队写入"| PARSE_Q
    PARSE_Q -->|"限流|解析并发"| SEM
    SEM -->|"限流|处理并发"| ENT_LOCK
    ENT_LOCK -->|"保护|合并写入"| VDB_LOCK & GRAPH_LOCK

    style LOCK1 fill:#ffcdd2,stroke:#c62828
    style LOCK2 fill:#fff3e0,stroke:#e65100
    style LOCK3 fill:#e3f2fd,stroke:#1565c0
    style LOCK4 fill:#e8f5e9,stroke:#2e7d32
```

---

## 七、状态流转网

文档在系统中的生命周期状态转换：

```mermaid
stateDiagram-v2
    [*] --> PENDING : enqueue 入队

    PENDING --> PARSING : parse_worker 取走

    PARSING --> ANALYZING : 解析完成→分析层
    PARSING --> FAILED : 解析异常

    ANALYZING --> PROCESSING : 分析完成→处理层
    ANALYZING --> FAILED : VLM 分析异常

    PROCESSING --> PROCESSED : extract + merge 完成
    PROCESSING --> FAILED : 抽取/合并异常

    FAILED --> PENDING : scan 重置重试

    PROCESSED --> [*] : 完成

    note right of PENDING
        并发契约:
        busy=True 不阻塞 enqueue
        destructive_busy 拒绝 enqueue
        scanning_exclusive 拒绝 enqueue
    end note

    note right of PROCESSING
        三子阶段:
        ① 分块 F/R/V/P
        ② extract_entities
        ③ merge_nodes_and_edges
    end note
```

---

## 八、数据流总览（一图打通读写）

把入库和查询放一起，看数据如何从输入流向输出：

```mermaid
graph LR
    subgraph INPUT_SIDE["📥 入库侧（写入）"]
        I1["文档"]
        I1 --> I2["解析"]
        I2 --> I3["分块"]
        I3 --> I4["抽取"]
        I4 --> I5["合并"]
    end

    subgraph DATA_STORE["💾 存储层（数据底座）"]
        direction TB
        DS1["full_docs<br/>doc_status"]
        DS2["text_chunks<br/>chunks_vdb"]
        DS3["entities_vdb<br/>full_entities"]
        DS4["relationships_vdb<br/>full_relations"]
        DS5["知识图谱"]
        DS6["llm_response_cache"]
    end

    subgraph OUTPUT_SIDE["📤 查询侧（读取）"]
        O1["关键词抽取"]
        O1 --> O2["KG 检索"]
        O1 --> O3["向量检索"]
        O2 --> O4["rerank"]
        O3 --> O4
        O4 --> O5["上下文组装"]
        O5 --> O6["LLM 生成"]
        O6 --> O7["答案"]
    end

    I2 -->|"写"| DS1
    I3 -->|"写"| DS2
    I4 -->|"写"| DS3 & DS4
    I4 -.->|"缓存"| DS6
    I5 -->|"写"| DS5 & DS3 & DS4

    O2 -->|"读"| DS3 & DS4 & DS5 & DS2
    O3 -->|"读"| DS2
    O6 -.->|"缓存"| DS6

    style INPUT_SIDE fill:#e8f5e9
    style DATA_STORE fill:#e3f2fd
    style OUTPUT_SIDE fill:#fff3e0
```

---

## 九、关键关系总结

### 9.1 谁持有谁

```mermaid
graph TD
    RAG["LightRAG 实例"]
    RAG -->|"持有"| S1["10 个存储实例"]
    RAG -->|"持有"| S2["4 个角色 LLM 配置"]
    RAG -->|"持有"| S3["embedding_func"]
    RAG -->|"持有"| S4["rerank_func（可选）"]
    RAG -->|"持有"| S5["parser registry 快照"]
    RAG -->|"持有"| S6["addon_params"]

    style RAG fill:#fffde7,stroke:#f57f17
```

### 9.2 关键调用链（最常走的路径）

```mermaid
graph LR
    subgraph 入库["入库最短路径"]
        A1["ainsert"] --> A2["enqueue"] --> A3["parse(F分块)"] --> A4["extract"] --> A5["merge"] --> A6["PROCESSED"]
    end

    subgraph 查询["查询最短路径(mix模式)"]
        Q1["aquery"] --> Q2["关键词"] --> Q3["KG+向量检索"] --> Q4["rerank"] --> Q5["LLM生成"] --> Q6["答案"]
    end

    style 入库 fill:#e8f5e9
    style 查询 fill:#fff3e0
```

### 9.3 组件耦合度

| 组件 | 耦合度 | 说明 |
|---|---|---|
| `operate.py` | **最高** | 被 pipeline 和 lightrag 双向依赖，核心枢纽 |
| `pipeline.py` | 高 | 依赖 parser/chunker/operate，被 lightrag 依赖 |
| `kg/` 后端 | 低 | 通过 base.py 契约解耦，可自由替换 |
| `llm/` provider | 低 | 通过角色路由解耦，可自由替换 |
| `parser/` 引擎 | 低 | 通过 registry 解耦，可插件注册 |

---

## 十、源码索引

| 关系图 | 关键源码位置 |
|---|---|
| LightRAG 持有存储 | `lightrag.py:1095-1145 initialize_storages` |
| 三级队列调度 | `pipeline.py:1143 _run_pipeline_batch` |
| 解析引擎路由 | `parser/registry.py:177 _REGISTRY` / `parser/routing.py` |
| 分块策略选择 | `pipeline.py:2188` |
| 实体抽取 | `operate.py:3320 extract_entities` |
| 合并入库 | `operate.py:2914 merge_nodes_and_edges` |
| KG 检索 | `operate.py:3786 kg_query` / `:4315 _perform_kg_search` |
| naive 检索 | `operate.py:5740 naive_query` |
| 角色定义 | `llm_roles.py:52 ROLES` |
| 锁机制 | `kg/shared_storage.py get_namespace_lock / get_storage_keyed_lock` |
| 状态机 | `base.py DocProcessingStatus` / `pipeline.py 各 worker` |

---

## 相关文档

- 解析流水线全流程详解：`解析流水线全流程详解.md`（侧重逐步执行细节）
- 核心执行链路与架构速览：`核心执行链路与架构速览文档.md`（侧重调用图）
- 技术栈与能力全景：`技术栈与能力全景.md`（侧重技术选型）
- 文档解析能力与输出格式对照：`文档解析能力与输出格式对照.md`（侧重格式）
