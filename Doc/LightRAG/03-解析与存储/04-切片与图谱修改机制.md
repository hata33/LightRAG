# LightRAG 切片与图谱修改机制

**项目**：LightRAG · **版本**：1.5.5 · **日期**：2026-07-10 · **作者**：15531

> 本文档回答：**改一个 chunk 怎么办？改整篇文档怎么办？前端需要维护切片吗？知识图谱支持修改吗？怎么改？** 全部基于源码核实（`lightrag.py:3111 adelete_by_doc_id`、`graph_routes.py`、`operate.py:1063 rebuild_knowledge_from_chunks`）。

---

## 一、核心结论先行

| 问题 | 答案 | 原因 |
|---|---|---|
| 改一个 chunk | **删旧 chunk → 重新入库新 chunk** | chunk 用内容哈希做 ID，内容变了就是新 chunk |
| 改整篇文档 | **删旧文档（级联清理切片+图谱）→ 重新解析入库** | `adelete_by_doc_id` 自动级联 |
| 前端要维护切片吗？ | **不需要精细到 chunk 级**，文档级管理足够 | chunk 是自动生成的，无独立生命周期 |
| 图谱支持修改吗？ | **支持，而且很完整** | 有完整 CRUD + 合并 API |
| 图谱怎么改？ | **API 调用 / WebUI 可视化编辑** | `update_entity` / `merge_entities` / `delete_entity` 等 |

---

## 二、LightRAG 的修改哲学：删-改-增

LightRAG **没有原地修改（update in place）**，一切修改都是**删 + 增**：

```mermaid
graph LR
    subgraph 原地修改["❌ 传统思路：原地修改"]
        U1["找到旧 chunk"]
        U1 --> U2["UPDATE content WHERE id=..."]
        U2 --> U3["❌ 问题：ID=MD5(content)<br/>内容变了 ID 就变了<br/>实体/关系的 source_id 失效"]
    end

    subgraph LIGHTRAG["✅ LightRAG 思路：删+增"]
        D1["删旧 chunk（级联清理）"]
        D1 --> D2["重新解析/入库"]
        D2 --> D3["生成新 chunk ID"]
        D3 --> D4["重新抽取实体/关系"]
    end

    style 原地修改 fill:#ffcdd2,stroke:#c62828
    style LIGHTRAG fill:#c8e6c9,stroke:#2e7d32
```

**为什么不能原地改**：chunk 的 ID 是 `MD5(content)`，内容一改 ID 就变，而图谱节点/边的 `source_id` 字段引用的是旧 chunk ID，会断裂。

---

## 三、改整篇文档：级联删除流程

`adelete_by_doc_id`（`lightrag.py:3111`）做的是**全链路级联清理**：

```mermaid
graph TD
    START["🗑️ 删除文档 doc_id"]

    START --> LOCK["获取管线锁<br/>━━━━━━━━━━━━━<br/>busy=True<br/>destructive_busy=True<br/>（拒绝并发上传）"]

    LOCK --> FIND["查找该文档的所有 chunk<br/>━━━━━━━━━━━━━<br/>doc_status.chunks_list"]

    FIND --> DEL_CHUNKS["删除切片<br/>━━━━━━━━━━━━━<br/>text_chunks.delete<br/>chunks_vdb.delete"]

    DEL_CHUNKS --> AFFECTED["找出受影响的实体/关系<br/>━━━━━━━━━━━━━<br/>哪些节点/边的 source_id<br/>引用了被删的 chunk"]

    AFFECTED --> CLASSIFY{"分类：删除还是重建?"}

    CLASSIFY -->|"source 全被删"| DEL_ENT["🗑️ 直接删除<br/>━━━━━━━━━━━━━<br/>这个实体只来自<br/>这份被删文档"]

    CLASSIFY -->|"source 部分被删"| REBUILD["🔄 重建<br/>━━━━━━━━━━━━━<br/>这个实体还来自<br/>其他文档<br/>用剩余 chunk 重新生成描述"]

    DEL_ENT --> CLEAN["清理图谱<br/>━━━━━━━━━━━━━<br/>graph 删节点/边<br/>entities_vdb.delete<br/>relationships_vdb.delete"]

    REBUILD --> REBUILD_FN["rebuild_knowledge_from_chunks<br/>━━━━━━━━━━━━━<br/>从剩余 chunk 重新抽取<br/>LLM 生成新描述<br/>重新 embed + upsert"]

    CLEAN --> DEL_DOC["删除文档主记录<br/>━━━━━━━━━━━━━<br/>full_docs.delete<br/>doc_status.delete"]

    REBUILD_FN --> DEL_DOC

    DEL_DOC --> DONE["✅ 完成"]

    style START fill:#ffcdd2,stroke:#c62828
    style DEL_ENT fill:#ffcdd2,stroke:#c62828
    style REBUILD fill:#fff3e0,stroke:#e65100
    style DONE fill:#c8e6c9,stroke:#2e7d32
```

### 关键设计：删除 vs 重建的判定（`lightrag.py:2801`）

```mermaid
graph TD
    ENTITY["一个受影响的实体<br/>（source_id 引用了被删 chunk）"]

    ENTITY --> CHECK["检查剩余 source"]

    CHECK --> Q1{"还有其他来源 chunk 吗?"}

    Q1 -->|"没有<br/>所有来源都被删"| DELETE["🗑️ 删除该实体<br/>━━━━━━━━━━━━━<br/>entities_to_delete.add()<br/>图谱删节点 + 向量库删"]

    Q1 -->|"有<br/>其他文档也提到了它"| REBUILD["🔄 重建该实体<br/>━━━━━━━━━━━━━<br/>entities_to_rebuild[name]=剩余sources<br/>rebuild_knowledge_from_chunks()<br/>用剩余 chunk 重新 LLM 抽取描述"]

    style DELETE fill:#ffcdd2,stroke:#c62828
    style REBUILD fill:#c8e6c9,stroke:#2e7d32
```

> **这个设计很聪明**：如果一个实体被多份文档提到，删其中一份不会丢失这个实体，而是用**剩余文档的 chunk 重新生成描述**。只有「只来自被删文档」的实体才会真正删除。

---

## 四、改一个 chunk：两种粒度

### 4.1 粗粒度（推荐）：文档级重处理

```mermaid
graph LR
    A["发现文档需要修改"] --> B["adelete_by_doc_id(doc_id)<br/>删整篇"]
    B --> C["重新 ainsert(新内容)<br/>或重新上传文件"]
    C --> D["系统重新解析+切片+抽取"]

    style B fill:#fff3e0,stroke:#e65100
    style C fill:#c8e6c9,stroke:#2e7d32
```

### 4.2 细粒度（SDK 支持）：单个 chunk 删除

```python
# LightRAG 没有「改一个 chunk」的 API，但可以：
# 1. 删旧 chunk
await rag.text_chunks.delete([old_chunk_id])
await rag.chunks_vdb.delete([old_chunk_id])
# 2. 加新 chunk（手动）
await rag.ainsert_custom_chunks(new_chunk_text, doc_id=doc_id)
# ⚠️ 但实体/关系不会自动更新（它们还引用旧 chunk_id）
```

> **细粒度修改有陷阱**：直接删 chunk 不会触发实体/关系的级联重建（只有 `adelete_by_doc_id` 才有级联逻辑）。所以**文档级重处理是推荐做法**。

---

## 五、前端切片维护：需要到什么粒度

```mermaid
graph TB
    subgraph 不需要["❌ 不需要前端维护的"]
        N1["单个 chunk 的增删改<br/>━━━━━━━━━━━━━<br/>chunk 是自动生成的<br/>无独立业务生命周期<br/>用户不该感知 chunk 边界"]
    end

    subgraph 需要["✅ 需要前端维护的"]
        direction LR
        Y1["文档级管理<br/>━━━━━━━━━━━━━<br/>上传 / 删除 / 重新上传<br/>查看处理状态"]
        Y2["图谱级管理<br/>━━━━━━━━━━━━━<br/>实体/关系 CRUD<br/>合并 / 重命名"]
        Y3["质量审查<br/>━━━━━━━━━━━━━<br/>查看抽取结果<br/>手动纠错"]
    end

    subgraph 可选["⚡ 可选增强"]
        direction LR
        O1["chunk 预览<br/>━━━━━━━━━━━━━<br/>只读查看分块结果<br/>（调试用，非编辑）"]
        O2["图谱可视化<br/>━━━━━━━━━━━━━<br/>Sigma.js 浏览子图<br/>（LightRAG WebUI 已有）"]
    end

    style 不需要 fill:#ffcdd2,stroke:#c62828
    style 需要 fill:#c8e6c9,stroke:#2e7d32
    style 可选 fill:#fff3e0,stroke:#e65100
```

### 前端应该提供的能力对照

| 操作 | 粒度 | 前端需要? | 底层 API |
|---|---|---|---|
| 上传文档 | 文档级 | ✅ 需要 | `POST /documents/upload` |
| 删除文档 | 文档级 | ✅ 需要 | `DELETE /documents/{doc_id}` |
| 查看文档状态 | 文档级 | ✅ 需要 | `GET /documents` |
| 重新上传（覆盖）| 文档级 | ✅ 需要 | 删后传 |
| 查看 chunk 内容 | chunk 级 | ⚡ 可选（只读） | full_docs 的 chunks_list |
| 编辑单个 chunk | chunk 级 | ❌ 不需要 | 无（且不推荐） |
| 编辑实体 | 图谱级 | ✅ 需要 | `POST /graph/entity` |
| 编辑关系 | 图谱级 | ✅ 需要 | `POST /graph/relation` |
| 合并实体 | 图谱级 | ✅ 需要 | `POST /graph/merge` |

---

## 六、知识图谱修改：完整的 CRUD

LightRAG 的图谱修改能力**非常完整**（`graph_routes.py`）：

```mermaid
graph TB
    subgraph ENTITY["实体操作"]
        direction LR
        CE["创建实体<br/>━━━━━━━━━<br/>POST /graph/entity<br/>create_entity()"]
        UE["更新实体<br/>━━━━━━━━━<br/>POST /graph/entity/update<br/>update_entity()<br/>可改名/改描述/改类型"]
        DE["删除实体<br/>━━━━━━━━━<br/>POST /graph/entity/delete<br/>delete_entity()<br/>级联删相关边"]
        ME["合并实体<br/>━━━━━━━━━<br/>POST /graph/merge<br/>merge_entities()<br/>两个实体合为一个"]
    end

    subgraph RELATION["关系操作"]
        direction LR
        CR["创建关系<br/>━━━━━━━━━<br/>POST /graph/relation<br/>create_relation()"]
        UR["更新关系<br/>━━━━━━━━━<br/>POST /graph/relation/update<br/>update_relation()<br/>改描述/权重"]
        DR["删除关系<br/>━━━━━━━━━<br/>POST /graph/relation/delete<br/>delete_relation()"]
    end

    subgraph QUERY["查询操作"]
        direction LR
        Q1["获取子图<br/>━━━━━━━━━<br/>GET /graph/knowledge_graph<br/>按深度/标签过滤"]
        Q2["搜索标签<br/>━━━━━━━━━<br/>GET /graph/label/search"]
        Q3["检查实体存在<br/>━━━━━━━━━<br/>GET /graph/entity/exists"]
    end

    style ENTITY fill:#e3f2fd,stroke:#1565c0
    style RELATION fill:#fff3e0,stroke:#e65100
    style QUERY fill:#e8f5e9,stroke:#2e7d32
```

---

## 七、图谱修改的底层机制

### 7.1 更新实体（`update_entity`，`graph_routes.py:250`）

```mermaid
graph TD
    REQ["POST /graph/entity/update<br/>━━━━━━━━━━━━━<br/>entity_name=张三<br/>updated_data={description: 新描述}<br/>allow_rename=false"]

    REQ --> FIND["图谱找到该节点"]
    FIND --> UPDATE["更新节点属性<br/>━━━━━━━━━━━━━<br/>graph_storage.upsert_node()<br/>改 description / type / source_id"]

    UPDATE --> REVDB["重新向量化<br/>━━━━━━━━━━━━━<br/>entities_vdb.upsert()<br/>新描述 → 重新 embed"]

    REVDB --> DONE["✅ 完成<br/>查询时召回新描述"]

    style REQ fill:#fffde7,stroke:#f57f17
    style REVDB fill:#fff3e0,stroke:#e65100
```

> 改了描述会**自动重新向量化**，保证后续查询用新描述召回。

### 7.2 合并实体（`merge_entities`，`graph_routes.py:649`）

```mermaid
graph LR
    BEFORE["合并前<br/>━━━━━━━━━<br/>'张三' (来源:docA)<br/>'Zhang San' (来源:docB)<br/>两个独立节点"]

    BEFORE --> MERGE["POST /graph/merge<br/>source=张三<br/>target=Zhang San"]

    MERGE --> COMBINE["合并过程<br/>━━━━━━━━━━━━━<br/>① 合并 description（LLM 生成）<br/>② 合并 source_id（docA+docB）<br/>③ 合并所有关联边<br/>④ 删除旧节点<br/>⑤ 创建合并后新节点"]

    COMBINE --> AFTER["合并后<br/>━━━━━━━━━<br/>'张三' (来源:docA+docB)<br/>一个节点，描述更完整<br/>重新 embed"]

    style MERGE fill:#fff3e0,stroke:#e65100
    style AFTER fill:#c8e6c9,stroke:#2e7d32
```

> 合并是**图谱维护的核心操作**——处理「同一实体被抽成不同名字」（张三 vs Zhang San）的同义问题。

### 7.3 删除实体（`delete_entity`，`graph_routes.py:737`）

```mermaid
graph TD
    DEL["POST /graph/entity/delete<br/>entity_name=张三"]

    DEL --> NODE["删除图谱节点<br/>━━━━━━━━━━━━━<br/>graph_storage.remove_node()"]
    DEL --> VDB["删除向量<br/>━━━━━━━━━━━━━<br/>entities_vdb.delete_entity()"]
    DEL --> EDGE["级联删除关联边<br/>━━━━━━━━━━━━━<br/>所有与张三相连的<br/>关系边也被删"]
    DEL --> FULL["清理 full_entities"]

    NODE & VDB & EDGE & FULL --> DONE["✅ 彻底删除"]

    style DEL fill:#ffcdd2,stroke:#c62828
    style DONE fill:#c8e6c9,stroke:#2e7d32
```

---

## 八、完整修改场景对照

```mermaid
graph TD
    SCENE["修改场景"]

    SCENE --> S1["场景 1：文档内容改了"]
    S1 --> S1_FLOW["adelete_by_doc_id → 重新 ainsert<br/>━━━━━━━━━━━━━<br/>✅ 自动级联清理<br/>✅ 自动重新抽取<br/>✅ 受影响实体自动重建"]

    SCENE --> S2["场景 2：实体描述不准确"]
    S2 --> S2_FLOW["POST /graph/entity/update<br/>━━━━━━━━━━━━━<br/>✅ 手动改 description<br/>✅ 自动重新 embed<br/>✅ 不影响 chunk"]

    SCENE --> S3["场景 3：同一实体两个名字"]
    S3 --> S3_FLOW["POST /graph/merge<br/>━━━━━━━━━━━━━<br/>✅ 合并为一个节点<br/>✅ LLM 合并描述<br/>✅ 合并 source_id"]

    SCENE --> S4["场景 4：错误的实体/关系"]
    S4 --> S4_FLOW["POST /graph/entity/delete<br/>或 /relation/delete<br/>━━━━━━━━━━━━━<br/>✅ 彻底删除<br/>✅ 级联清理"]

    SCENE --> S5["场景 5：补充新关系"]
    S5 --> S5_FLOW["POST /graph/relation<br/>━━━━━━━━━━━━━<br/>✅ 手动创建<br/>✅ 自动 embed"]

    style S1_FLOW fill:#e3f2fd,stroke:#1565c0
    style S2_FLOW fill:#c8e6c9,stroke:#2e7d32
    style S3_FLOW fill:#fff3e0,stroke:#e65100
    style S4_FLOW fill:#ffcdd2,stroke:#c62828
    style S5_FLOW fill:#f3e5f5,stroke:#6a1b9a
```

---

## 九、前端维护建议

### 9.1 推荐的前端能力分层

```mermaid
graph TB
    subgraph L1["第一层：文档管理（必须）"]
        direction LR
        L1A["上传"]
        L1B["列表/状态"]
        L1C["删除"]
        L1D["重新上传"]
    end

    subgraph L2["第二层：图谱编辑（推荐）"]
        direction LR
        L2A["实体增删改"]
        L2B["关系增删改"]
        L2C["实体合并"]
        L2D["可视化浏览"]
    end

    subgraph L3["第三层：质量审查（可选）"]
        direction LR
        L3A["chunk 只读预览"]
        L3B["抽取结果审查"]
        L3C["Bad case 标注"]
    end

    L1 --> L2 --> L3

    style L1 fill:#e3f2fd,stroke:#1565c0
    style L2 fill:#c8e6c9,stroke:#2e7d32
    style L3 fill:#fff3e0,stroke:#e65100
```

### 9.2 为什么不做 chunk 级编辑

| 理由 | 说明 |
|---|---|
| chunk 无业务语义 | 切片边界由算法决定，用户不该关心第 3 个 chunk 在哪 |
| 修改破坏哈希 | ID=MD5(content)，改内容→ID 变→source_id 断裂 |
| 无级联 | 删单个 chunk 不会触发实体重建，只有文档级删除才有 |
| 成本不划算 | 改一个 chunk 的复杂度 ≈ 重新入库整篇文档 |

---

## 十、操作示例

### 10.1 删文档重新入库（Python SDK）

```python
# 删旧文档（自动级联清理切片+图谱）
await rag.adelete_by_doc_id("doc-xxx")
# 重新入库
await rag.ainsert(new_content, ids=["doc-xxx"])
```

### 10.2 图谱修改（REST API）

```bash
# 更新实体描述
curl -X POST http://localhost:9621/graph/entity/update \
  -H "Authorization: Bearer <JWT>" \
  -d '{"entity_name":"张三","updated_data":{"description":"更新后的描述"}}'

# 合并两个实体
curl -X POST http://localhost:9621/graph/merge \
  -d '{"source_entity":"张三","target_entity":"Zhang San"}'

# 删除关系
curl -X POST http://localhost:9621/graph/relation/delete \
  -d '{"source_id":"张三","target_id":"李四"}'
```

### 10.3 自定义 KG 导入（跳过抽取，手动建图谱）

```python
custom_kg = {
    "entities": [
        {"entity_name": "张三", "description": "...", "source_id": "chunk-xxx"},
    ],
    "relationships": [
        {"src_id": "张三", "tgt_id": "李四", "description": "同事", "source_id": "chunk-xxx"},
    ],
}
await rag.ainsert_custom_kg(custom_kg)
```

---

## 十一、源码索引

| 能力 | 源码位置 |
|---|---|
| 文档级联删除 | `lightrag.py:3111 adelete_by_doc_id` |
| 删除 vs 重建判定 | `lightrag.py:2801` 分类逻辑 |
| 用剩余 chunk 重建实体 | `operate.py:1063 rebuild_knowledge_from_chunks` |
| 实体 CRUD API | `graph_routes.py:250 update_entity` / `:481 create_entity` / `:737 delete_entity` |
| 实体合并 | `graph_routes.py:649 merge_entities` |
| 关系 CRUD API | `graph_routes.py:443 update_relation` / `:557 create_relation` / `:773 delete_relation` |
| 自定义 KG 导入 | `lightrag.py:1786 ainsert_custom_kg` |

---

## 相关文档

- 切片存储设计：`切片存储设计.md`（chunk 如何落库）
- 流水线流程与网状关系：`流水线流程与网状关系.md`（存储读写角色）
- 项目架构图：`项目架构图.md`（整体分层）
- 作为 RAG 基座融合指南：`作为RAG基座与MCP工具的融合指南.md`（SDK/API 调用）
