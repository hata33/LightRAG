# LightRAG 切片存储设计

**项目**：LightRAG · **版本**：1.5.5 · **日期**：2026-07-10 · **作者**：15531

> 本文档详解 LightRAG **文件识别 → 切片 → 存储**的设计：一份文档如何被拆分、切出来的 chunk 携带哪些字段、如何同时落库到**关系型数据库（PostgreSQL）**和**向量库（pgvector）**、以及表结构细节。全部基于源码核实（`lightrag.py:1513`、`base.py:72`、`kg/postgres_impl.py:7964`）。

---

## 一、全景：从文件到存储

一份文档从进入系统到落库，经过五个阶段，产出**关系库 + 向量库**双写：

```mermaid
graph LR
    FILE["📄 原始文件<br/>PDF/Word/Excel/..."]

    FILE --> S1["① 解析<br/>━━━━━━━━━<br/>parser 引擎<br/>→ Markdown/纯文本"]
    S1 --> S2["② 切片<br/>━━━━━━━━━<br/>chunker 策略<br/>F/R/V/P"]
    S2 --> S3["③ 生成 chunk 元数据<br/>━━━━━━━━━━━━━<br/>id/content/tokens/<br/>order/full_doc_id"]

    S3 --> S4["④ 并行双写"]
    S4 --> REL["关系库 LIGHTRAG_DOC_CHUNKS<br/>━━━━━━━━━━━━━<br/>全文 + 元数据<br/>供精确查找/引用"]
    S4 --> VEC["向量库 LIGHTRAG_VDB_CHUNKS<br/>━━━━━━━━━━━━━<br/>embedding + 元数据<br/>供语义召回"]

    S3 --> S5["⑤ 抽取实体/关系<br/>━━━━━━━━━━━━━<br/>→ 图谱 + 实体/关系向量"]

    style FILE fill:#fffde7,stroke:#f57f17
    style REL fill:#e3f2fd,stroke:#1565c0
    style VEC fill:#fff3e0,stroke:#e65100
    style S5 fill:#e8f5e9,stroke:#2e7d32
```

**关键设计：同一份 chunk 数据，同时写两张表**——关系库存全文+元数据，向量库存 embedding+元数据。两者用相同的 `id`（chunk 的 MD5）关联。

---

## 二、切片：一份文档切成什么

### 2.1 切片过程（`lightrag.py:1538`）

```mermaid
graph TD
    DOC["文档全文 full_text"]

    DOC --> CHUNKER["分块器<br/>━━━━━━━━━<br/>按策略切分"]
    CHUNKER --> STRATEGY{"策略"}
    STRATEGY -->|"F"| F["固定 token 大小<br/>CHUNK_SIZE=1200"]
    STRATEGY -->|"R"| R["递归字符<br/>按分隔符层级"]
    STRATEGY -->|"V"| V["语义向量<br/>embedding 聚类"]
    STRATEGY -->|"P"| P["段落语义<br/>依赖 blocks.jsonl"]

    F & R & V & P --> CHUNKN["chunk 文本列表<br/>text_chunks: list[str]"]

    CHUNKN --> LOOP["遍历每个 chunk"]
    LOOP --> META["生成元数据<br/>━━━━━━━━━━━━━<br/>id = MD5(chunk_text)<br/>content = chunk_text<br/>tokens = tokenizer.encode<br/>chunk_order_index = 序号<br/>full_doc_id = 文档ID<br/>file_path = 来源路径"]

    META --> DICT["inserting_chunks 字典<br/>━━━━━━━━━━━━━<br/>{'chunk-xxx': {元数据}}"]

    style DOC fill:#fffde7,stroke:#f57f17
    style META fill:#fff3e0,stroke:#e65100
    style DICT fill:#c8e6c9,stroke:#2e7d32
```

### 2.2 chunk 的数据结构（`base.py:72`）

每个切片是一个 `TextChunkSchema`，携带以下字段：

| 字段 | 类型 | 来源 | 用途 |
|---|---|---|---|
| `id` | str | `compute_mdhash_id(chunk_text, "chunk-")` | 全局唯一键（内容哈希） |
| `content` | str | 分块器输出 | 切片全文 |
| `tokens` | int | `len(tokenizer.encode(chunk_text))` | token 数（预算控制） |
| `full_doc_id` | str | 文档的 MD5 (`doc-xxx`) | 反查所属文档 |
| `chunk_order_index` | int | `enumerate` 序号 | 切片在文档中的顺序 |
| `file_path` | str | 原始文件路径 | 引用溯源 |

> **内容哈希去重**：相同内容 → 相同 `id` → `filter_keys` 自动跳过已存在的 chunk（`lightrag.py:1551`），避免重复入库。

---

## 三、存储设计：关系库 vs 向量库

### 3.1 双写架构（`lightrag.py:1559-1565`）

切片生成后，**四个任务并行执行**（`asyncio.gather`）：

```mermaid
graph TD
    CHUNKS["inserting_chunks<br/>{chunk-id: {content, tokens, ...}}"]

    CHUNKS --> T1["chunks_vdb.upsert<br/>━━━━━━━━━━━━━<br/>① embedding_func 编码 content<br/>② 写 LIGHTRAG_VDB_CHUNKS"]
    CHUNKS --> T2["text_chunks.upsert<br/>━━━━━━━━━━━━━<br/>直接写 LIGHTRAG_DOC_CHUNKS<br/>（无 embedding）"]
    CHUNKS --> T3["full_docs.upsert<br/>━━━━━━━━━━━━━<br/>写文档全文 LIGHTRAG_DOC_FULL"]
    CHUNKS --> T4["_process_extract_entities<br/>━━━━━━━━━━━━━<br/>抽取实体/关系<br/>→ 图谱 + 实体/关系向量"]

    T1 --> EMBED["embedding_func<br/>━━━━━━━━━<br/>content → np.ndarray<br/>text-embedding-3 / bge-m3 / ..."]

    style CHUNKS fill:#fffde7,stroke:#f57f17
    style T1 fill:#fff3e0,stroke:#e65100
    style T2 fill:#e3f2fd,stroke:#1565c0
    style EMBED fill:#f3e5f5,stroke:#6a1b9a
```

### 3.2 关系库表：LIGHTRAG_DOC_CHUNKS

**存什么**：切片全文 + 元数据，**不含向量**。供精确查找、引用溯源、顺序还原。

```sql
CREATE TABLE LIGHTRAG_DOC_CHUNKS (
    id                 VARCHAR(255),      -- chunk-<MD5>，内容哈希
    workspace          VARCHAR(255),      -- 多租户隔离
    full_doc_id        VARCHAR(256),      -- 所属文档 doc-<MD5>
    chunk_order_index  INTEGER,           -- 切片在文档中的序号
    tokens             INTEGER,           -- token 数
    content            TEXT,              -- 切片全文
    file_path          TEXT NULL,         -- 来源文件路径（引用溯源）
    llm_cache_list     JSONB DEFAULT '[]',-- LLM 缓存引用
    heading            JSONB DEFAULT '{}',-- 标题层级（parent_headings）
    sidecar            JSONB DEFAULT '{}',-- 多模态引用（图片/表格/公式）
    create_time        TIMESTAMP,
    update_time        TIMESTAMP,
    CONSTRAINT LIGHTRAG_DOC_CHUNKS_PK PRIMARY KEY (workspace, id)
);
```

**主键设计**：`(workspace, id)` —— workspace 实现多租户，id 是内容哈希保证幂等。

### 3.3 向量库表：LIGHTRAG_VDB_CHUNKS

**存什么**：切片的 embedding + 精简元数据。供语义召回（naive 查询、KG 检索的 chunk 补充）。

```sql
CREATE TABLE LIGHTRAG_VDB_CHUNKS (
    id              VARCHAR(255),         -- chunk-<MD5>，与 DOC_CHUNKS 关联
    workspace       VARCHAR(255),
    full_doc_id     VARCHAR(256),         -- 所属文档
    chunk_order_index INTEGER,
    tokens          INTEGER,
    content         TEXT,                 -- 原文（冗余存，便于召回后直接用）
    content_vector  VECTOR(dimension),    -- ← pgvector，embedding 维度
    file_path       TEXT NULL,
    create_time     TIMESTAMP,
    update_time     TIMESTAMP,
    CONSTRAINT LIGHTRAG_VDB_CHUNKS_PK PRIMARY KEY (workspace, id)
);

-- 向量索引（HNSW 最常用，cosine 相似度）
CREATE INDEX idx_vdb_chunks_vector
    ON LIGHTRAG_VDB_CHUNKS USING hnsw (content_vector vector_cosine_ops)
    WITH (m = 16, ef_construction = 200);
```

> **`VECTOR(dimension)`** 的 dimension 由 embedding 模型决定（text-embedding-3-small=1536, bge-m3=1024）。建表时动态替换。

### 3.4 两表关系

```mermaid
graph LR
    DOC_FULL["LIGHTRAG_DOC_FULL<br/>━━━━━━━━━<br/>id: doc-xxx<br/>content: 全文"]

    DOC_CHUNKS["LIGHTRAG_DOC_CHUNKS<br/>━━━━━━━━━<br/>id: chunk-xxx<br/>full_doc_id: doc-xxx<br/>content: 切片全文<br/>❌ 无向量"]

    VDB_CHUNKS["LIGHTRAG_VDB_CHUNKS<br/>━━━━━━━━━<br/>id: chunk-xxx（相同）<br/>content_vector: embedding<br/>content: 切片全文"]

    DOC_FULL -->|"1:N<br/>full_doc_id"| DOC_CHUNKS
    DOC_CHUNKS -->|"1:1<br/>相同 id"| VDB_CHUNKS

    style DOC_FULL fill:#e8f5e9,stroke:#2e7d32
    style DOC_CHUNKS fill:#e3f2fd,stroke:#1565c0
    style VDB_CHUNKS fill:#fff3e0,stroke:#e65100
```

**关联方式**：两表用**相同的 `id`（chunk 的内容哈希）**关联，而非外键。关系库存全字段，向量库只存召回必需字段 + 向量。

---

## 四、实体/关系的存储设计

切片不仅直接入库，还触发**实体/关系抽取**，产生更多存储写入：

```mermaid
graph TD
    CHUNK["chunk 切片"]

    CHUNK --> EXTRACT["extract_entities<br/>━━━━━━━━━━━━━<br/>LLM 抽取实体 + 关系"]

    EXTRACT --> ENT["实体"]
    EXTRACT --> REL["关系"]

    ENT --> ENT_VEC["LIGHTRAG_VDB_ENTITY<br/>━━━━━━━━━━━━━<br/>entity_name + content_vector<br/>chunk_ids: 来源切片列表"]
    ENT --> ENT_FULL["LIGHTRAG_FULL_ENTITIES<br/>━━━━━━━━━━━━━<br/>entity_names JSONB<br/>（详情快照）"]
    ENT --> GRAPH_NODE["知识图谱 节点<br/>━━━━━━━━━━━━━<br/>Apache AGE<br/>name + description + source_id"]

    REL --> REL_VEC["LIGHTRAG_VDB_RELATION<br/>━━━━━━━━━━━━━<br/>source_id + target_id + content_vector<br/>chunk_ids: 来源切片"]
    REL --> REL_FULL["LIGHTRAG_FULL_RELATIONS<br/>━━━━━━━━━━━━━<br/>relation_pairs JSONB"]
    REL --> GRAPH_EDGE["知识图谱 边<br/>━━━━━━━━━━━━━<br/>source→target + weight + description"]

    style CHUNK fill:#fffde7,stroke:#f57f17
    style ENT fill:#e3f2fd,stroke:#1565c0
    style REL fill:#fff3e0,stroke:#e65100
    style GRAPH_NODE fill:#e8f5e9,stroke:#2e7d32
    style GRAPH_EDGE fill:#e8f5e9,stroke:#2e7d32
```

### 实体向量表

```sql
CREATE TABLE LIGHTRAG_VDB_ENTITY (
    id              VARCHAR(255),
    workspace       VARCHAR(255),
    entity_name     VARCHAR(512),         -- 实体名
    content         TEXT,                 -- 实体描述（用于 embed）
    content_vector  VECTOR(dimension),    -- 实体描述的 embedding
    chunk_ids       VARCHAR(255)[] NULL,  -- ← 来源切片 ID 数组（溯源）
    file_path       TEXT NULL,
    ...
);
```

### 关系向量表

```sql
CREATE TABLE LIGHTRAG_VDB_RELATION (
    id              VARCHAR(255),
    workspace       VARCHAR(255),
    source_id       VARCHAR(512),         -- 源实体名
    target_id       VARCHAR(512),         -- 目标实体名
    content         TEXT,                 -- 关系描述（用于 embed）
    content_vector  VECTOR(dimension),    -- 关系描述的 embedding
    chunk_ids       VARCHAR(255)[] NULL,  -- ← 来源切片 ID 数组（溯源）
    file_path       TEXT NULL,
    ...
);
```

> **`chunk_ids` 数组**是关键溯源字段：实体/关系来自哪些切片，能反查回原文。

---

## 五、完整的表关系图（ER 图）

PostgreSQL 全栈部署时的所有表及其关系：

```mermaid
graph TB
    subgraph 文档层
        DOC_FULL["LIGHTRAG_DOC_FULL<br/>━━━━━━━━━<br/>id: doc-MD5<br/>content: 全文<br/>content_hash<br/>parse_engine"]
        DOC_STATUS["LIGHTRAG_DOC_STATUS<br/>━━━━━━━━━<br/>id: doc-MD5<br/>status: PENDING→PROCESSED<br/>chunks_list<br/>chunks_count"]
    end

    subgraph 切片层
        DOC_CHUNKS["LIGHTRAG_DOC_CHUNKS<br/>━━━━━━━━━<br/>id: chunk-MD5<br/>content: 切片全文<br/>tokens / order / heading<br/>sidecar（多模态引用）"]
        VDB_CHUNKS["LIGHTRAG_VDB_CHUNKS<br/>━━━━━━━━━<br/>id: chunk-MD5<br/>content_vector: VECTOR"]
    end

    subgraph 实体关系层
        VDB_ENTITY["LIGHTRAG_VDB_ENTITY<br/>━━━━━━━━━<br/>entity_name<br/>content_vector<br/>chunk_ids[]"]
        FULL_ENT["LIGHTRAG_FULL_ENTITIES<br/>━━━━━━━━━<br/>entity_names JSONB"]
        VDB_REL["LIGHTRAG_VDB_RELATION<br/>━━━━━━━━━<br/>source_id / target_id<br/>content_vector<br/>chunk_ids[]"]
        FULL_REL["LIGHTRAG_FULL_RELATIONS<br/>━━━━━━━━━<br/>relation_pairs JSONB"]
    end

    subgraph 图谱层
        AGE["Apache AGE 图谱<br/>━━━━━━━━━<br/>节点: 实体<br/>边: 关系<br/>(name, description, source_id)"]
    end

    subgraph 缓存层
        CACHE["LIGHTRAG_LLM_CACHE<br/>━━━━━━━━━<br/>original_prompt<br/>return_value<br/>chunk_id"]
    end

    subgraph 索引层
        ENT_CHUNKS["LIGHTRAG_ENTITY_CHUNKS<br/>━━━━━━━━━<br/>id: entity_name<br/>chunk_ids JSONB<br/>(实体→切片反向索引)"]
    end

    DOC_FULL -->|"full_doc_id"| DOC_CHUNKS
    DOC_FULL -->|"full_doc_id"| VDB_CHUNKS
    DOC_CHUNKS -->|"1:1 相同id"| VDB_CHUNKS
    DOC_CHUNKS -->|"chunk_ids[]"| VDB_ENTITY
    DOC_CHUNKS -->|"chunk_ids[]"| VDB_REL
    DOC_STATUS -->|"id"| DOC_FULL
    VDB_ENTITY --> AGE
    VDB_REL --> AGE
    ENT_CHUNKS -->|"反向"| DOC_CHUNKS

    style 文档层 fill:#e8f5e9,stroke:#2e7d32
    style 切片层 fill:#e3f2fd,stroke:#1565c0
    style 实体关系层 fill:#fff3e0,stroke:#e65100
    style 图谱层 fill:#f3e5f5,stroke:#6a1b9a
    style 缓存层 fill:#fce4ec,stroke:#c62828
```

---

## 六、workspace 多租户设计

所有表都有 `workspace` 列，主键都是 `(workspace, id)`：

```mermaid
graph LR
    subgraph 租户隔离
        direction TB
        RULE["WHERE workspace = ? <br/>━━━━━━━━━━━━━<br/>每个查询都带 workspace 过滤<br/>同一张物理表，逻辑隔离"]
    end

    subgraph 示例
        direction LR
        WS_A["workspace='project_a'<br/>chunk-1, chunk-2, ..."]
        WS_B["workspace='project_b'<br/>chunk-3, chunk-4, ..."]
        WS_C["workspace=''（默认）<br/>chunk-5, chunk-6, ..."]
    end

    RULE --> 示例

    style RULE fill:#fff3e0,stroke:#e65100
```

> 同一个 PG 实例、同一张表，不同 `workspace` 数据互不可见。切换 workspace 不影响已有数据。

---

## 七、查询时如何用这些存储

不同的检索模式读取不同的表：

```mermaid
graph TD
    QUERY["🔍 查询"]

    QUERY --> NAIVE["naive 模式<br/>━━━━━━━━━<br/>只查 LIGHTRAG_VDB_CHUNKS<br/>content_vector 召回 chunk"]

    QUERY --> KG["local/global/hybrid/mix<br/>━━━━━━━━━━━━━<br/>多表联查"]

    KG --> K1["① LIGHTRAG_VDB_ENTITY<br/>向量召回相关实体"]
    KG --> K2["② LIGHTRAG_VDB_RELATION<br/>向量召回相关关系"]
    KG --> K3["③ Apache AGE 图谱<br/>get_node_edges 图遍历"]
    KG --> K4["④ LIGHTRAG_VDB_CHUNKS<br/>召回相关 chunk（补充）"]

    K1 & K2 & K3 --> FIND["_find_related_text_unit<br/>━━━━━━━━━━━━━<br/>通过 chunk_ids[] 反查<br/>LIGHTRAG_DOC_CHUNKS 取全文"]
    K4 --> FIND

    FIND --> CTX["组装上下文<br/>━━━━━━━━━━━━━<br/>实体/关系/chunk 全文<br/>token 预算截断"]

    style QUERY fill:#fffde7,stroke:#f57f17
    style NAIVE fill:#e3f2fd,stroke:#1565c0
    style KG fill:#fff3e0,stroke:#e65100
    style FIND fill:#c8e6c9,stroke:#2e7d32
```

**关键链路**：向量召回实体/关系 → 它们的 `chunk_ids[]` → 反查 `LIGHTRAG_DOC_CHUNKS` 拿切片全文 → 组装进上下文。

---

## 八、设计要点总结

```mermaid
graph TB
    subgraph 设计决策
        D1["① 内容哈希做主键<br/>━━━━━━━━━━━━━<br/>chunk-id = MD5(content)<br/>天然去重 + 幂等<br/>相同内容不重复入库"]
        D2["② 关系库与向量库双写<br/>━━━━━━━━━━━━━<br/>关系库: 全字段/引用/溯源<br/>向量库: embedding/召回<br/>用相同 id 关联"]
        D3["③ workspace 列多租户<br/>━━━━━━━━━━━━━<br/>所有表带 workspace<br/>同一物理表逻辑隔离"]
        D4["④ chunk_ids 溯源链<br/>━━━━━━━━━━━━━<br/>实体/关系记录来源切片<br/>可反查原文证据"]
        D5["⑤ embedding 在写入时算<br/>━━━━━━━━━━━━━<br/>chunks_vdb.upsert 内部<br/>调 embedding_func 编码<br/>存 content_vector"]
        D6["⑥ 并行写入<br/>━━━━━━━━━━━━━<br/>asyncio.gather 同时<br/>写关系库/向量库/全文/抽取"]
    end

    style D1 fill:#e3f2fd,stroke:#1565c0
    style D2 fill:#e8f5e9,stroke:#2e7d32
    style D3 fill:#fff3e0,stroke:#e65100
    style D4 fill:#f3e5f5,stroke:#6a1b9a
    style D5 fill:#fffde7,stroke:#f57f17
    style D6 fill:#fce4ec,stroke:#c62828
```

---

## 九、源码索引

| 设计点 | 源码位置 |
|---|---|
| chunk 数据结构 | `base.py:72 TextChunkSchema` |
| chunk 生成 + 元数据 | `lightrag.py:1538-1548` |
| 并行双写（关系库+向量库+全文+抽取） | `lightrag.py:1559-1565 asyncio.gather` |
| 内容哈希去重 | `lightrag.py:1540 compute_mdhash_id` + `:1551 filter_keys` |
| embedding 编码入口 | `chunks_vdb.upsert`（内部调 `embedding_func`） |
| LIGHTRAG_DOC_CHUNKS DDL | `postgres_impl.py:7988` |
| LIGHTRAG_VDB_CHUNKS DDL | `postgres_impl.py:8005` |
| LIGHTRAG_VDB_ENTITY DDL | `postgres_impl.py:8020` |
| LIGHTRAG_VDB_RELATION DDL | `postgres_impl.py:8034` |
| HNSW 索引配置 | `postgres_impl.py` `POSTGRES_VECTOR_INDEX_TYPE=HNSW` |
| workspace 过滤 | `postgres_impl.py` 所有查询的 `WHERE workspace=$1` |

---

## 相关文档

- 项目架构图：`项目架构图.md`（整体分层）
- 流水线流程与网状关系：`流水线流程与网状关系.md`（存储读写角色）
- 文档解析能力与输出格式对照：`文档解析能力与输出格式对照.md`（解析阶段）
- 技术栈与能力全景：`技术栈与能力全景.md`（存储后端清单）
